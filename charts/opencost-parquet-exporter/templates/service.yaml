apiVersion: v1
kind: Service
metadata:
  name: {{ include "opencost-parquet-exporter.fullname" . }}
  labels:
    {{- include "opencost-parquet-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opencost-parquet-exporter.selectorLabels" . | nindent 4 }}
